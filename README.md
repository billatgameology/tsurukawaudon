# Tsurukawaudon

Simple local-run homepage.

## Run locally
Open `index.html` in your browser (double-click), or use a local server:

```bash
cd "/Users/billw/Code Files/tsurukawaudon"
python3 -m http.server 8080
```

Then open `http://localhost:8080`.
